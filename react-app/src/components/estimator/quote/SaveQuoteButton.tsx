/**
 * SaveQuoteButton — Placeholder until Supabase is configured
 * Renders nothing for now. Will be enabled once we add
 * @supabase/supabase-js and set VITE_SUPABASE_URL + VITE_SUPABASE_ANON_KEY.
 */
export function SaveQuoteButton() {
  return null;
}

export default SaveQuoteButton;
